# APK Output Directory

Place the generated `bloom-v1.apk` file here after running the EAS build.

See `README.md` in the project root for full build instructions.

## Expected file
`bloom-v1.apk` — release APK built with EAS Build (profile: `production`)

## After placing the APK here
The file will be available as `builds/bloom-v1.apk` as requested.
